"use strict";
var expect = require('chai').expect;
// example for remember
describe('Simple math test', function () {
    it('Should return 2', function () {
        expect(1 + 1).to.equal(2);
    });
    it('Shold return 9', function () {
        expect(3 * 3).to.equal(9);
    });
});
